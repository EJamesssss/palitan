<div class="row">
  <div class="col-sm-13">
    <div class="card-header">
      <div class="card-body-categ">
        <h2>List of Users</h2>
        <div class="add-users">
          <a class="nav-link bi-plus-square" href="#"> Add User</a>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="table-box"> 
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>EMAIL</th>
        <th>ROLE</th>
        <th>STATUS</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @registered_users.each do |user|%>
        <tr>
          <td><%= user.email%></td>
          <td><%= user.admin === true ? "Admin" : "Trader"%></td>
          <td><%= user.approved === false ? "Pending" : "Approved"%></td>
          <td><%= link_to ' Show', dashboard_path(user), class: "btn btn-show bi-eye-fill" %></td>
        </tr>
      <%end%>
    </tbody>
  </table>
</div>
