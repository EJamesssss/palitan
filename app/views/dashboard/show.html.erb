<div class="box"> 
    <div class="card text-center">
        <div class="card-body">
            <p>Email Address</p>
            <h4><%= @registered_users.email%></h4>

            <p>Role</p>
            <h4><%= @registered_users.admin === true ? "Admin" : "Trader"%></h4>

            <p>Status</p>
            <h4><%= @registered_users.approved === false ? "Pending" : "Approved"%></h4>
            <br/>
            
        </div>
        <div class="card-footer">
            <%= link_to ' Back', dashboard_index_path, class: "btn btn-cancel bi-arrow-left-short space" %>
        </div>
    </div>
</div>