<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
<%= image_tag "logo.png", height: 60 %>
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if user_signed_in? && current_user.approved%>
                    <% if current_user.admin%>

                        <li class="nav-item">
                            <%= link_to "Transaction History", transactions_path, class: "nav-link"%>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link"></a>
                        </li> 

                        <li class="nav-item">
                            <%= link_to "Portfolio", portfolio_index_path, class: "nav-link"%>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link"></a>
                        </li> 

                        <li class="nav-item">
                            <%= link_to "List of Users", admin_users_path, class: "nav-link"%>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link"></a>
                        </li> 

                        <li class="nav-item">
                            <%= link_to "Unapproved Users", pending_users_path, class: "nav-link"%>
                        </li>
                    <% else %>
                        <li class="nav-item">
                            <%= link_to "Transaction History", transactions_path, class: "nav-link"%>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link"></a>
                        </li> 

                        <li class="nav-item">
                            <%= link_to "Portfolio", portfolio_index_path, class: "nav-link"%>
                        </li>
                    <% end %>
                <% end %>
            </ul>
            <div class="d-flex">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                   
                    <% if user_signed_in? %>
                        <li class="nav-item dropdown">
                            <a class="nav-link">
                                <i class="icon-x bi-person-circle"></i> 
                                Hi, <%= current_user.email %>
                            </a>
                            <div class="dropdown-content">
                                <%= link_to ' Edit Profile', edit_user_registration_path, class:"nav-link bi-pencil-square" %>
                                <%= link_to " Sign Out", destroy_user_session_path, method: :delete, class:"nav-link bi-box-arrow-right" %>
                            </div>
                        </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </div>
</nav>